namespace Bittrex {
    public enum OrderBookType {
        Buy,
        Sell,
        Both
    }

    public enum FillType {
        Fill,
        PartialFill
    }

    public enum OrderType {
        Buy,
        Sell
    }

    public enum OpenOrderType {
        LimitBuy,
        LimitSell
    }
}
